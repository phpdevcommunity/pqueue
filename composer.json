{
    "name": "phpdevcommunity/pqueue",
    "description": "PQueue is a minimalist PHP library for processing background messages using a single persistent CLI (managed via systemd) or periodic execution (via cron), covering 90% of use cases without external dependencies or complex worker management.",
    "type": "library",
    "license": "MPL-2.0",
    "authors": [
        {
            "name": "F. Michel"
        }
    ],
    "autoload": {
        "psr-4": {
            "PhpDevCommunity\\PQueue\\": "src",
            "Test\\PhpDevCommunity\\PQueue\\": "tests"
        }
    },
    "require": {
        "php": ">=7.4",
        "ext-sqlite3": "*",
        "ext-json": "*",
        "phpdevcommunity/php-console": "^1.0",
        "phpdevcommunity/michel-package-starter": "^1.0",
        "psr/log": "^1.1|^2.0|^3.0"
    },
    "require-dev": {
        "phpdevcommunity/unitester": "^0.1.0@alpha"
    }
}
